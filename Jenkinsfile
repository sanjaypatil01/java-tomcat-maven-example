pipeline {
    agent any
    stages {
        stage ('initialized') {
            steps {
                /*For windows machine */
               echo "M2_HOME = %M2_HOME%"
               echo "Initializing the code" 
                           
            }
        }

        stage ('Build') {
            steps {
                echo 'Hello World ....!'                  
            }
        }    
    }
}